/**
 * Stub for modifiers-napi (native keyboard modifier detection).
 * Only works in Bun with native bindings.
 */
export function prewarm() {}
export function isModifierPressed(_m: string): boolean { return false }
