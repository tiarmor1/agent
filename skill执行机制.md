## 开头

一句话概述 Skills 原理：本质上是一种**能力分层暴露机制**：模型在初始阶段只看到少量、高层级的能力入口，而非全部具体实现细节；当某个能力被触发后，才进一步加载其细化定义或具体工具描述。

宏观表述来看，Skills 就是一个自组织的文件夹。在 Anthropic 的这篇《Equipping agents for the real world with Agent Skills》中有比较具象的展示，大致可分为三段式：

- 极小的 MetaData，用于描述工具的作用和功能，以 YAML 格式存在。
- 完整的主体能力描述，Markdown 形式存在。
- 文件夹内的其他文件、脚本的（可选），可被主体文件通过名称声明使用。

在 Agent 框架在启动时，会扫描所有的安装的 Skills，并将其 metadata（Skills Desciption）添加到 System Prompt 中。 简易实现和结构示意图如下：

```python
class SkillRegistry:
    """
    1. 发现并注册本地 Skills
    2. 提供 Skill 元数据（供系统构建能力清单）
    3. 按需加载 Skill 完整内容
    """

    def __init__(self, skills_dir: Path):
        self.skills_dir = skills_dir
        self._registry: dict[str, dict] = {}
        self._discover()

    def _discover(self) -> None:
        """扫描目录并注册所有 SKILL.md"""

    def _parse_skills_file(self, path: Path) -> dict:
        """解析 Skill manifest（frontmatter + body）"""

    def list_metadata(self) -> str:
        """返回能力摘要（注入系统提示词）"""
        return "\n".join(
            f"- {name}: {meta['description']}"
            for name, meta in self._registry.items()
        )

    def load_content(self, name: str) -> str:
        """按需加载指定 Skill 的完整内容"""
        skill = self._registry[name]
        return f"# Skill: {name}\n\n{skill['body']}"
```

## 参考文章

https://www.anthropic.com/engineering/equipping-agents-for-the-real-world-with-agent-skills

## 文章总结

### 标题

让智能体在真实世界中具备可落地能力：用 Agent Skills 为智能体装备专业技能

### 核心观点

随着模型能力不断增强，通用型智能体已经可以在完整的计算环境中工作，例如访问文件系统、执行本地代码、完成跨领域任务。但如果想让这类智能体真正适应现实工作场景，只靠模型本身的通用知识还不够，还需要一种更可组合、可扩展、可移植的方式，把特定领域的经验、流程和工具能力装配给智能体。

Anthropic 为此提出了 Agent Skills。Skill 本质上是一个目录，里面包含 `SKILL.md`、补充文档、脚本和相关资源。它不是重新为每个场景训练或搭建一个新智能体，而是把人的操作经验和流程知识打包成可被智能体按需发现、按需加载的能力模块。这样，通用智能体就能通过装配 Skill，变成更贴合具体业务的专用智能体。

作者把 Skill 类比为“给新员工准备的一套入职手册”。与其为每个场景都定制一个碎片化、彼此割裂的专用 Agent，不如把流程知识、工作规范、工具脚本整理成可复用的能力包，让智能体在需要时自己找到并使用。

### 什么是 Skill

一项 Skill 是一个目录，其中必须包含一个 `SKILL.md` 文件。这个文件承载了该 Skill 的核心说明。目录中还可以包含其他被引用的文件，以及供智能体执行的脚本和资源。

Skill 的价值在于：它把领域知识、执行步骤、参考资料和工具代码都组织进统一结构里，让智能体以低上下文成本获得额外能力。

### Skill 的结构设计

文章重点介绍了 Skill 的分层结构，也就是所谓的“渐进式披露”（progressive disclosure）。

#### 第一层：元数据

`SKILL.md` 文件开头必须包含 YAML Frontmatter，其中至少要有两个关键字段：

- `name`
- `description`

系统在启动时，会把所有已安装 Skill 的 `name` 和 `description` 预加载进系统提示词。这一步只加载最小必要信息，目的是让模型先知道“有哪些技能可用，以及它们大概适用于什么场景”，而不是一开始就把所有 Skill 的完整内容全部塞进上下文。

这就是第一层渐进式披露：只提供让智能体做出触发判断所需的最低成本信息。

#### 第二层：完整的 SKILL.md

当智能体根据当前任务判断某个 Skill 可能相关时，它才会进一步读取该 Skill 的完整 `SKILL.md` 内容，把更详细的操作规则、使用方式和说明加载进上下文。

也就是说，Skill 并不是默认全部展开的，而是由模型自行判断是否需要深入读取。

#### 第三层及更多层：补充文档和资源

如果一个 Skill 变得复杂，单个 `SKILL.md` 可能会太长，或者其中有些内容只在少数情景下才需要。这时可以把这些内容拆到其他文件中，再由 `SKILL.md` 按名称引用它们。

比如文章里的 PDF Skill，把某些与表单填写相关的说明拆到 `forms.md` 中。这样做的好处是：

- 保持核心 `SKILL.md` 精简
- 把互斥或低频场景拆出去
- 让智能体只在真的需要时再读取对应文件
- 节省上下文窗口中的 token 成本

作者认为，这种“目录 -> 核心说明 -> 按需展开子文档”的方式，是 Skill 可以同时做到灵活和可扩展的关键。

### 为什么渐进式披露很重要

文章把 Skill 的组织方式类比成一本结构良好的手册：

- 开头先给目录
- 然后进入具体章节
- 最后才看附录和细节

对于具备文件系统和代码执行能力的智能体来说，这意味着它并不需要在处理任务前，把整个 Skill 的所有说明都读进上下文窗口。只要它能够动态读取文件，就可以在需要时再加载相关部分。因此，理论上一个 Skill 所能承载的信息量几乎是不受上下文窗口硬限制的。

这也是 Skill 与传统“把全部说明硬塞进一个长提示词”相比的根本优势：它把知识管理从一次性加载，变成了按需检索和按需展开。

### Skill 在上下文窗口中的工作方式

文章还解释了 Skill 被触发时，上下文是如何变化的。整体流程可以概括为：

1. 一开始，上下文里只有系统提示词、所有已安装 Skill 的元数据，以及用户的请求。
2. 模型看到用户请求后，判断某个 Skill 相关。
3. 它通过工具读取该 Skill 的 `SKILL.md`。
4. 如果 `SKILL.md` 中又提到了额外文件，它再继续按需读取这些文件。
5. 加载了相关指导后，模型再继续执行用户任务。

也就是说，Skill 的触发不是靠外部硬编码路由，而是依靠模型根据元数据和任务语义自行决定何时加载、加载到什么深度。

### Skill 与代码执行的关系

文章强调，Skill 不只是“说明文档”。它还可以内嵌脚本代码，让智能体自行调用。

原因很简单：大模型虽然擅长理解和推理，但有些任务更适合交给传统程序处理。例如：

- 排序、提取、转换这类确定性操作
- 需要稳定重复结果的工作
- 不希望靠 token 逐字生成去模拟算法的场景

作者举的例子是 PDF Skill 内置了一个 Python 脚本，可以直接读取 PDF 并提取表单字段。智能体不需要把脚本本身或整个 PDF 文件都加载进上下文，只需运行脚本，再根据脚本输出继续工作。

这种模式带来的好处包括：

- 更高效，避免用大模型去做代码更擅长的事
- 更稳定，执行结果可重复
- 更节省上下文，减少无谓信息进入 prompt

因此，Skill 的理想形态不是纯文本知识包，而是“说明 + 参考资料 + 可执行代码”的组合。

### 文中示例：PDF Skill

为了解释 Skill 的结构，文章以一个真实的 PDF Skill 为例。Claude 本身已经很擅长理解 PDF 内容，但它原生并不擅长直接操作 PDF，比如填写表单。借助这个 PDF Skill，可以给 Claude 增加以下能力：

- 读取并理解 PDF 处理规则
- 在需要时查看更细的表单处理说明
- 调用脚本提取 PDF 中的表单字段
- 根据提取结果完成更复杂的文档编辑任务

这个例子说明了 Skill 的核心价值：不是替代模型，而是补齐模型在某一具体工作流中的能力短板。

### 如何开发和评估 Skill

文章给出了一组非常实用的实践建议。

#### 1. 从评估开始

不要先拍脑袋写 Skill，而应该先让智能体在真实任务上跑起来，观察它卡在哪里、缺什么上下文、在哪些地方经常出错。然后再有针对性地补 Skill。

也就是说，Skill 应该建立在真实缺口之上，而不是建立在想象中的“也许以后会需要”之上。

#### 2. 为扩展性组织结构

当 `SKILL.md` 变长、变乱时，就应该拆文件。特别是那些互斥场景、低频上下文，最好分开存放，降低一次性加载的成本。

此外，代码文件既可以作为可执行工具，也可以作为参考文档的一部分。Skill 作者需要写清楚：哪些脚本应该直接运行，哪些文件只是让模型阅读理解。

#### 3. 从 Claude 的视角设计 Skill

作者提醒，要关注模型真实是怎么使用 Skill 的，而不是只从作者自己的想象出发设计它。尤其是 `name` 和 `description`，因为模型正是依赖这两个字段来决定要不要触发 Skill。

如果元数据命名含糊、描述不准，即使 Skill 本体写得很好，模型也可能根本不会触发它，或者在错误场景下触发它。

#### 4. 和 Claude 一起迭代 Skill

一个很有意思的建议是：可以在与 Claude 一起完成任务时，让 Claude 自己把成功经验、容易出错的点、可复用流程逐步沉淀到 Skill 中。

如果 Claude 在使用 Skill 时走偏了，也可以让它反思究竟是哪里出了问题。通过这种方式，Skill 会逐渐从“作者预设的知识包”，演化成“在真实工作中验证过、不断迭代优化的操作资产”。

作者的意思其实很明确：不要试图一开始就闭门造车写出完美 Skill，而要在实际使用中不断修正，逐步发现模型真正需要的上下文。

### 安全性考虑

文章专门强调了 Skill 的安全风险。因为 Skill 可以给 Claude 增加新能力，也意味着恶意 Skill 可能：

- 引入有问题的脚本或依赖
- 指导模型连接不可信的外部网络
- 诱导模型外泄数据
- 让模型执行用户并不希望发生的操作

因此，Anthropic 的建议是：

- 只安装来自可信来源的 Skill
- 如果来源不完全可信，使用前要彻底审计
- 认真检查 Skill 自带的文件、代码依赖、脚本、图片等资源
- 特别注意其中是否包含让模型访问外部网络、连接不可信服务、上传本地数据等行为

这部分其实是在提醒：Skill 的能力越强，治理要求也越高。它不是单纯的提示词模板，而是可能带有执行能力的扩展包，因此必须像审查脚本和第三方插件一样审查 Skill。

### Skill 的未来

文章最后展望了 Skill 的发展方向。

当前，Agent Skills 已经支持：

- Claude.ai
- Claude Code
- Claude Agent SDK
- Claude Developer Platform

接下来，Anthropic 计划继续完善 Skill 的完整生命周期支持，包括：

- 创建
- 编辑
- 发现
- 分享
- 使用

作者尤其看好 Skill 在组织和个人知识沉淀方面的作用。它不仅可以把“会做某事”变成“可复用地教会智能体做某事”，还可以把团队流程、组织经验、业务规范沉淀成可以长期复用的能力资产。

文章还提到，未来 Skill 会与 MCP（Model Context Protocol）形成互补关系。MCP 更偏向让智能体接入外部工具和系统，而 Skill 更适合教智能体如何在这些工具和系统上执行更复杂的工作流。换句话说：

- MCP 更像“接上工具”
- Skill 更像“教会怎么用工具完成具体任务”

再往后看，Anthropic 希望智能体自己也能创建、编辑和评估 Skill，把它们自己在工作中形成的有效模式，沉淀成可复用的新能力。这意味着 Skill 不只是人给智能体写的外挂，未来也可能成为智能体自我优化、自我积累能力的载体。

### 总结性的理解

整篇文章想表达的重点，可以归纳为下面几句：

1. 通用智能体已经越来越强，但离真实业务落地还差“领域经验”和“工作流能力”这一步。
2. Agent Skills 提供了一种简单、结构化、可扩展的方法，把人的经验、说明文档、脚本工具和参考资料打包给智能体。
3. Skill 的关键设计原则是渐进式披露，即只在需要时加载最小必要信息，从而降低上下文成本、提升可扩展性。
4. Skill 不只是文本说明，也可以包含可执行代码，让模型把推理和传统程序能力结合起来。
5. 好的 Skill 不是一次性设计出来的，而是在真实任务、真实失败和真实迭代中逐步打磨出来的。
6. Skill 很强大，但也必须重视安全治理，因为它可能改变智能体的执行边界。

### 我的补充理解

如果用一句更接地气的话来概括，这篇文章真正提出的是一种“给智能体做能力产品化封装”的方法论。

过去常见的做法是：

- 要么把大量规则直接塞进提示词
- 要么为每个场景单独做一个 Agent
- 要么靠人手动告诉模型每一步怎么做

而 Skill 的做法是：

- 用目录结构组织能力
- 用元数据做发现和触发
- 用 `SKILL.md` 做核心指导
- 用附加文档承载分支知识
- 用脚本承载确定性执行能力
- 用渐进式加载控制上下文成本

这套机制的价值，不只在于“让模型更聪明”，更在于“让组织经验可以被稳定复用、共享、迁移和维护”。
